{% extends 'base.html.twig' %}
{% form_theme form 'form/imagenForm.html.twig' %}
{% block titulo %}Registro Usuario{% endblock %}
{% block contenido %}
<div class="container">

<h1 class="mt-4 mb-3"> Nuevo Usuario</h1>   
<div class="row">       
     <div class="col-lg-6">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
    <div class="col-md-6">
        <h2 class="fw-bolder">Our founding</h2>
        <p class="lead fw-normal text-muted mb-0">Si usted ya está registrado, entonces haga click aquí <a href="{{ path('login') }}" class="nav-link">Volver a Login</a></p>
    </div>
</div>
</div>
        
</section>

    



{% endblock %}

{% block javascripts %}
  <script src="{{ asset('js/nuevaImg.js') }}">
{% endblock %}